<% if controller_name == "welcome" %>
<nav class="navbar navbar-default navbar-fixed-top">
<% else  %>
<nav class="navbar navbar-default navbar-fixed-top navbar-shrink">
<% end  %>
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header page-scroll">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand page-scroll" href="<%= root_path %>"><img id="nav-logo" src="https://i.imgur.com/dI0IeAJ.png"></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li class="hidden">
                    <a href="#page-top"></a>
                </li>

                <% if user_signed_in? %>
                  <li>
                      <%= link_to "Exécuter une course", new_availability_path %>
                  </li>
                  <li>
                      <%= link_to "Passer commande",mes_commandes_path %>
                  </li>

                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Profil   <img src="<%= current_user.picture.url(:medium)%>" class="img-circle img-profile-nav" ></a>
                      <ul id="login-dp" class="dropdown-menu">
                        <li class="text-center">
                                <a href="<%= user_path(current_user) %>" >Mon profil  </a>
                        </li>
                        <li class="text-center">
                                  <a href="#" >Mes Commandes</a>
                        </li>
                        <li class="text-center">
                                  <a href="#" >Mes Avis</a>
                        </li>
                      </ul>


                  </li>

                  <li>
                      <%= link_to "Logout", destroy_user_session_path, method: :delete %>
                  </li>
                <% else %>
                  <li>
                      <a class="page-scroll" href="#accueil">Accueil</a>
                  </li>
                  <li>
                      <a class="page-scroll" href="#what">Comment ça marche ?</a>
                  </li>
                  <li>
                      <a class="page-scroll" href="#why">Pourquoi nous ?</a>
                  </li>
                  <li>
                      <a class="page-scroll" href="#about">About</a>
                  </li>
                  <li>
                      <a class="modalLink" href="#">Login</a>
                  </li>
                  <li>
                      <%= link_to "Sign up", new_user_registration_path %>
                  </li>
                <% end %>

            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
