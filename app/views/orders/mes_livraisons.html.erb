<h1>Vos Livraison</h1>

<table class="table table_striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Shopper</th>
      <th>status d'execution</th>
      <th>total</th>
      <th>commission</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.orders.each_with_index do |order, index| %>
      <tr>
        <td><%= order.date %></td>
        <td><% if order.status_exec == true %>
              <%= order.total_amount %>
            <% else %>
              <%=  "Indisponible" %></td>
              <%end%>
        <td><% if order.status_exec == true %>
              <%= order.commission %>
            <% else %>
               <%=  "Indisponible" %></td>
              <%end%>
        <td><% if order.status_exec == true %>
              <%= "Effectuée" %>
            <% else %>
              <%= "En cours" %></td>
        <td><%= order.commission%></td>
            <%end%>
  <% end %>
  </tbody>
</table>
